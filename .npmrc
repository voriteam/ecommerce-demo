legacy-peer-deps=true
enable-pre-post-scripts=true
# The template pins engines to Node >=24.15.0; keep that advisory so a
# contributor on Node 22 gets a warning rather than a hard install failure.
engine-strict=false
